/**
 * Google Tag Manager container.
 *
 * Loaded in app/layout.tsx after the Consent Mode v2 default-denied snippet
 * so GTM honors denied until Accept. Shares the same dataLayer name as the
 * existing Ads/GA4 gtag.js tags — do not add a second gtag.js here.
 */
export const GTM_ID = "GTM-5LL47PDP";
